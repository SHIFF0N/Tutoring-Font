import users from './users';
import roles from './roles';


const state = {};

const mutations = {};

const actions = {};

export default {
    namespaced: true,
    state,
    mutations,
    actions,
    modules: {
        users: users,
        roles: roles,
    }

}
